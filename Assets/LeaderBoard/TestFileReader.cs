using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TestFileReader : MonoBehaviour {

   /* public string dataFile = "[]data.txt";
    public List<string> data = new List<string>();

    public List<string> files = new List<string>();

    public string[] excludeExtensions = new string[] { ".meta" };

    public string searchExtension = ".txt";

    public bool search = false;

    public Text score1, score2, score3, score4, score5;


    private void Start()
    {

        /*score1.text = "SUPERGUY : 1000000000";
        score2.text = "Trollman : 900000000";
        score3.text = "Standoff : 800000000";
        score4.text = "Soldier1 : 700000000";
        score5.text = "Minecraf : 600000000";

        FileHandler.SaveFileWithLines(dataFile, new List<string>() { score1.text, score2.text, score3.text, score4.text, score5.text});

        data = FileHandler.LinesOfFile(dataFile);

        search = true;
        //files = FileHandler.GetAllFilesInDirectory("[]files/", excludeExtensions, searchExtension);

    }

    private void Update()
    {
        if (search)
        {
            search = false;
            Search();
        }
    }

    void Search()
    {
        files = FileHandler.GetAllFilesInDirectory("[]files/", excludeExtensions, searchExtension);
    }*/
}
